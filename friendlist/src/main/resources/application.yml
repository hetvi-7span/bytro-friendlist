# SERVER CONFIGURATION
server:
  port: 8092
  servlet:
    context-path: "/${spring.application.name}"

# SPRING CONFIGURATION
spring:
  application:
    name: friendlist
  # SPRING JPA CONFIGURATION
  datasource:
    url: jdbc:mariadb://0.0.0.0:3306/friendlist_management
    username: root
    password: root
  jpa:
    open-in-view: false
    hibernate:
      dialect: org.hibernate.dialect.MariaDBDialect
      ddl-auto: none
  jackson:
      default-property-inclusion: NON_NULL
  flyway:
    url: jdbc:mariadb://localhost:3306/friendlist_management
    user: root
    password: root
  mail:
    host: smtp.gmail.com
    transport:
      protocol: smtp
    username: test.friendlist.123@gmail.com
    password: ejbzjgqnupdqfuxb
    smtp:
      debug: true
      starttls:
        enable: true
      auth: true
      ssl:
        protocols: TLSv1.2
        trust: smtp.gmail.com

# LOGGING CONFIGURATION
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE

#custom properties
request:
  mail:
    subject : New friend alert
    receiver : shahhetvi252@gmail.com



